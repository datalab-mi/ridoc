
#https://blog.bam.tech/developer-news/dockerize-your-app-and-keep-hot-reloading

server {
    listen       80;
    listen 35729;
    server_name  localhost;


    location / {
        proxy_pass http://<FRONTEND_DEV_HOST>:<FRONTEND_DEV_PORT>;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        #proxy_cache_bypass $http_upgrade;
        #break;
    }

    location /api {
        proxy_pass http://<BACKEND_HOST>:<BACKEND_PORT>/;
    }

    location /elasticsearch/ {
        proxy_pass http://<ES_HOST>:<ES_PORT>/;
        proxy_redirect off;
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        break;

    }

    location ~ "/livereload.*" {
    	proxy_pass http://browser-frontend-dev:35729;
      # Allow protocol upgrade to websocket
      proxy_http_version 1.1;
      proxy_next_upstream off;
      proxy_set_header Host $http_host;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
